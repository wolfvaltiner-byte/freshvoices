# Fresh Voices — Verlauf 2026

Ausgelagert aus `CLAUDE.md` am 2026-08-31, wörtlich unverändert. Was hier steht, ist
erledigt: es erklärt, *wie* etwas entstanden ist, nicht wie man heute arbeitet.

---

## WP-03: Mobile Hero neu geordnet — Text zuerst, Video-Poster, Showreel-Badge (2026-09-06)

Umsetzung der Opus-Entscheidung aus `docs/review-2026-09/WP-03-mobile-hero.md` (Leitplanken-Abschnitt): mobil zeigt der Hero jetzt Text → CTA zuerst, Video und Audio-Card folgen darunter — nicht mehr davor.

- **Mobile Reihenfolge**: `.hero__visual { order: -1; }` entfernt (in `src/css/pages/index.css`) — die DOM-Reihenfolge (`.hero__content` vor `.hero__visual` in `src/index.njk`) war schon immer Text-zuerst, nur der visuelle CSS-Override hatte sie umgedreht. `h1` liegt jetzt im ersten Viewport bei `top ≈ 143px` (iPhone 13 und iPhone-17-ähnlich, jeweils gemessen), Primär-CTA „Hörproben anhören" endet bei `bottom ≈ 462px`/`466px` — beide klar unter der 640px-Vorgabe.
- **Hero-Höhe mobil**: `min-height:100vh/100svh` + `padding-top:80px` gelten jetzt nur noch `@media (min-width:769px)`; mobil `min-height:auto`, `padding-top: calc(var(--nav-h) + env(safe-area-inset-top) + 24px)`, `padding-bottom:48px` — der Hero streckt sich mobil nicht mehr künstlich auf Viewport-Höhe.
- **Headline & Subline mobil**: `.hero__title` auf `clamp(2.4rem, 11vw, 3.2rem)`, `line-height:1.0`; `.hero__subtitle` per `-webkit-line-clamp:3` auf drei sichtbare Zeilen begrenzt, damit der Primär-CTA sicher im ersten Viewport bleibt.
- **Video-Poster** (neu, damit vor dem Autoplay-Start kein leeres Schwarzbild erscheint): `src/images/hero-poster.jpg` (12.4 KB) und `.webp` (4.9 KB), 1280×724, per `ffmpeg -ss 1.0 … -vf scale=1280:-2` aus `videos/production-hook.mp4` extrahiert; `poster="images/hero-poster.jpg"` auf dem `<video>`-Element gesetzt. Neues Front-Matter-Flag `preloadHero: true` (nur auf `index.njk` gesetzt) lässt `base.njk` bedingt `<link rel="preload" as="image" href="images/hero-poster.jpg" fetchpriority="high">` in den `<head>` schreiben.
- **Video-Container mobil**: `aspect-ratio:16/9`, `object-fit:cover`, `max-height:45svh` (gemessen: 197px tatsächliche Höhe bei 390px Breite, weit unter dem 45svh-Deckel von ~299px). Desktop bleibt bei `aspect-ratio:4/5`.
- **Badge**: „Video-Hook“ → „Showreel · 25 s“ (DE/EN identisch) — reale Videolänge per `ffprobe` gemessen (25.03s, gerundet).
- **Mute-Button Tap-Ziel**: mobil auf `44×44px` vergrößert (Icon bleibt optisch 18px, zentriert per Flexbox).
- **Audio-Card**: `margin-top` mobil von 36px auf 24px reduziert, volle Breite (unverändert vom Layout her, war bereits Block-Element).
- **Stats-Leiste**: Inhalt unverändert (Wolfs Entscheidung zu „2 Sprachen/48h“ vs. Kunden-Logo-Strip steht noch aus), mobil `padding` 40px→28px und Grid-`gap` auf 24px reduziert.
- **Ungeplanter Fund + Fix**: das Setzen von `poster` deckte einen vorbestehenden Layout-Bug auf — `.hero` ist `display:flex` mit einem einzigen Kind (`.container`) ohne eigene Breite; ohne `poster` hatte das `<video>` eine winzige intrinsische Default-Größe (300×150 als Replaced Element), wodurch der Flex-Item-Container sich nach Content-Fit auf nur ~1072px statt der vollen 1320px einpendelte — **die rechte Spalte (Video + Audio-Card) war auf Desktop dadurch faktisch unsichtbar** (dunkles Video auf dunklem Hintergrund, genau das in `CLAUDE.md` dokumentierte „Traps that already bit“-Muster). Mit `poster` (großes intrinsisches Maß 1280×724) sprang der Container plötzlich auf volle Breite — das Video wurde sichtbar, aber das Vorher/Nachher-Bild änderte sich massiv statt nur im Badge-Text. Fix: `.hero > .container { width: 100%; }` in `src/css/pages/index.css` erzwingt die volle verfügbare Breite unabhängig vom intrinsischen Video-Maß — entspricht der eigentlich beabsichtigten symmetrischen Zwei-Spalten-Struktur.
- **Verifikation**: `npm run build` clean. Playwright (iPhone 13 390×664 dsf3, iPhone-17-ähnlich 402×874 dsf3): `h1.top<300` ✓, CTA `bottom≤innerHeight` und `≤640px` ✓, `scrollWidth===innerWidth` ✓ (kein horizontaler Overflow), Video-`top` > CTA-`bottom` ✓ auf beiden Geräten. Desktop 1440px: Struktur (zwei Spalten, Inhalte) unverändert, aber Pixel-Diff der Hero-Region liegt bei ~49% statt < 3% — Ursache und Screenshots siehe oben/`WP-03-mobile-hero.md`. Mobile-Menü-Test aus WP-01 wiederholt (Hamburger öffnen/schließen, Escape, `html.menu-open`) — weiterhin grün. Lighthouse-LCP-Messung nicht durchgeführt (kein Tooling in der Sandbox); Poster ist jetzt aber das LCP-Element statt des Videos.
- **Nicht committet/gepusht**: diese Session hat auf explizite Dateien committet, aber nicht gepusht (siehe WP-03-Karte für offene Rückfrage an Wolf zum Container-Breite-Fix).

### Folge-Commit "WP-03 fix: no subline clamp, cap desktop video height" (2026-09-06)

Zwei Nacharbeiten nach Review-Feedback:

- **Mobile Subline nicht mehr per `-webkit-line-clamp` abgeschnitten** (der Satz „Auf Deutsch und…" wirkte fehlerhaft) — volle Subline steht wieder da, Platz kompensiert über `font-size:1rem` und engere Abstände statt Kürzung. CTA bleibt mit `bottom=440px` (iPhone 13, `innerHeight=664`) klar im ersten Viewport.
- **Desktop-Video-Höhe gedeckelt**: das volle 4/5-Video war bei ~620px Spaltenbreite ~775px hoch, klebte am Header und schob die Audio-Card unter den Fold. `.hero__photo-wrap` bekommt ab 769px eine feste `height:min(56vh,520px)` plus `width:fit-content; align-self:center`, `.hero__photo` wird per `height:100%; width:auto; aspect-ratio:4/5; object-fit:cover` reinskaliert — zentriert statt rechtsbündig, damit Badge/Mute-Button symmetrisch im Video-Block bleiben statt an die Spaltenkante zu rutschen.
- **Gemessen**: 1440×900 — Lücke Header→Video 66.6px, Audio-Card endet bei 832.9px (< 900); 1280×720 (Laptop) — Lücke 27.1px, Audio-Card endet bei 692.5px (< 720). Beide: Video **und** Audio-Card vollständig sichtbar, Badge bleibt innerhalb des Containers, kein horizontaler Overflow. Mobile- und WP-01-Menütest erneut grün. Screenshots: `desktop-hero-after2.png`, `desktop-hero-1280.png`, `mobile-top-iphone13-2.png` in `/home/claude/work/wp03-shots/`.

## WP-05: Bildoptimierung (2026-09-06)

Scopes 1–5 umgesetzt (Review WP-05): about.html-Bilder mit responsive WebP+JPG `<picture>`-Markup optimiert, ungenutzte Assets in `_unused/` geparkt.

- **Scope 1–2**: `images/Portrait.jpg` (3,5 MB) → `portrait-420.webp`, `portrait-840.webp`, `portrait-840.jpg` (Gesamt 396 KB, 89% Ersparnis); `images/Motto.jpg` (4,5 MB) → `motto-192.webp`, `motto-192.jpg` (Gesamt 8,4 KB, 100% Ersparnis aus Zentral-Crop und Verkleinerung auf 192×192). About-page: Bilder jetzt 404 KB gesamt (< 1 MB ✓).
- **Scope 3**: Ungenutzte Dateien nach `_unused/` verschoben (mit Dateinamenberechtigung für Leerzeichen→Bindestriche): `mic and plopp.jpg`, `with Mic.jpg`, `fresh-demos-cover.jpg`, `seeanoli-image.mp4`, `wolf-alpha-master.mp4`.
- **Scope 4**: PNG-Optimierung (verlustfrei) für Logos > 20 KB: `radio-klassik-transparent.png` (-17 B), `radio-klassik.png` (-142 B), `oecolution.png` (-656 B); OVERDUBs keine Einsparungen.
- **Scope 5**: `docs/ASSETS.md` mit vollständiger Dokumentation aktualisiert (Vorher/Nachher-Tabellen, Liste `_unused/`-Dateien).
- **Verifikation**: ✓ Alle Bildvarianten vorhanden, ✓ responsives `<picture>`/`srcset` in about.html, ✓ Playwright bestätigt WebP auf Desktop und iPhone 13.

---

## Mobile testing (2026-07-27)

Tested every page (index, samples, services, about, clients, contact) at iPhone 13 and Pixel 5 viewports via Playwright device emulation: zero horizontal overflow, zero console errors, hamburger menu opens/closes and locks body scroll correctly, 48×48px tap targets on the audio player controls (meets both iOS HIG and Android Material minimums), `playsinline`+`muted` already set on the autoplaying hero video (required for iOS inline autoplay), and `.hero`'s `min-height: 100vh` (not `height`) avoids the classic iOS Safari dynamic-toolbar clipping issue.

**False-alarm pattern worth knowing about**: full-page mobile screenshots of this site look like they have huge blank gaps where whole sections (service cards, use-case list, CTA banners, logo walls, the contact form) are missing. They aren't — every one of these was checked directly via computed styles/bounding boxes and the content, elements, and layout are all present and correctly sized. The illusion comes from stacking three things: (1) `.reveal`/`.revealed` scroll-triggered fade-in (elements sit at `opacity:0` until their `IntersectionObserver` fires), (2) the dark-on-dark color palette, and (3) viewing a full-page screenshot of a very tall mobile page downscaled 6–8× to fit a preview — at that scale, correctly-rendered muted-gray-on-near-black content becomes visually imperceptible. If something looks missing on mobile, check a targeted element screenshot or computed style before concluding it's a bug — this cost real time twice in this session.

Only one real bug turned up (see the "Nav button contrast bug" note above) — everything else mobile-specific checked out clean.

## Audit + critique fixes (2026-07-28)

Ran a full technical audit and a dual-agent design critique (`/impeccable audit` + `/impeccable critique`), then fixed everything that was a code-level bug rather than a business/asset decision:

- **Bilingual system now actually covers the whole site.** Nav links (`Home`/`Voice Samples`/`Services`/`About`/`Clients`/CTA button) and every footer on samples/services/about/clients/contact.html were hardcoded English or German-only with no `data-de`/`data-en` — only index.html's footer had it. All 6 pages now match index.html's footer exactly (including the social icons + address line it previously had and the others didn't) and all nav links are bilingual.
- **about.html's biography no longer desyncs from the rest of the page on language toggle.** A duplicate inline `<script>` at the bottom of about.html toggled `[data-lang-block]` on click only, registered *before* `main.js`'s listener, so it always read the button's stale pre-toggle text — the bio text was reliably one click behind the nav/facts/buttons, and never synced at all on page load (so a returning EN-preferring visitor, via the `fv-lang` localStorage persistence, saw the whole biography still in German). Fixed by moving `[data-lang-block]` handling into `main.js`'s single `applyLang()` and deleting the duplicate script.
- **Contact form validation actually runs now.** The `<form>` had `required`/`minlength`/`type="email"` but no `novalidate`, so the browser's native (monolingual) validation intercepted `submit` before the custom bilingual `validateForm()` ever ran — that whole code path was dead. Added `novalidate`, plus live `blur`/`input` feedback per field (was submit-only before).
- **Form error text now passes WCAG AA contrast.** `.form-error`/`.form-feedback.is-error` used `#e85454` on `--card` (#2E2E2E) — measured 3.77:1, below the 4.5:1 minimum. Replaced with a new `--error: #F27E7E` token (5.21:1). Also tokenized the footer's hardcoded `#111111` as `--footer-bg`.
- **Audio play/pause buttons now update their accessible name.** All ~13 `.audio-player__play` buttons kept a static `aria-label` ("Play"/"Play video demo") forever, even while playing — a WCAG 4.1.2 violation. Now toggles Play↔Pause in the label alongside the icon swap, matching the hero mute button's already-correct pattern.
- **clients.html heading hierarchy fixed.** "Kunden" and "Studios & Agenturen" were `<h3>`s appearing before the page's first `<h2>` — promoted both to `<h2>` so heading levels no longer skip.
- **Hamburger menu touch target widened** from ~32×24px to a full 44×44px hit area (icon itself unchanged, just centered via flex in the larger button box).
- **Hero "Video-Demo" label renamed to "Hörprobe"/"Audio demo"** — it was labeled as video content but is actually an `.audio-player` playing an mp3; carried over unfixed from the 2026-06-28 critique until now.
- **`nav__lang` button's `aria-label="Switch language"`** was only on index.html; now on all 6 pages.
- **Dead code removed**: the `.testimonial` CSS component (unused by any page, and its `border-left` accent was exactly the side-stripe anti-pattern the design system should avoid) is deleted outright.
- **Two layout-thrash CSS transitions fixed**: `.nav.scrolled` no longer animates `padding` (snaps instantly now, background/blur still animates smoothly); `.audio-player__bar`'s progress fill now animates `transform: scaleX()` instead of `width`, updated on every `timeupdate` tick.
- **clients.html's 15 `.logo-wall` images** now have real `width`/`height` (extracted from each file's actual PNG/SVG dimensions) and `loading="lazy"`, matching the `.ref-strip` images on the same page that already had both.
- **about.html's portrait** (`Portrait.jpg`) switched from `loading="lazy"` to `fetchpriority="high"` — it's above the fold on desktop, lazy-loading it was working against itself.

**Not fixed — flagged in `next_tasks.md` as Wolf's call, not a code fix**: Dokumentation and E-Learning & Training are priced on services.html/index.html with zero audio proof anywhere on samples.html. (clients.html's VOICE association card *was* a text-only placeholder at the time of this audit — since replaced with a real logo, see below.)

## VOICE association logo (2026-07-28)

Replaced clients.html's `.assoc-logo-placeholder` (a dashed box literally containing the word "VOICE") with the real VOICE/Sprecherverband logo. Sourced directly from sprecherverband.at rather than the VOICE pricelist PDF, since the live site had a cleaner vector source: their Drupal theme serves a CSS sprite at `/themes/custom/voice/assets/img/sprite_voice.svg` containing several copies of the mark at different sizes; the cleanest instance was isolated, rendered at high resolution via headless Chromium, then color-keyed (global chroma-key on white, same approach as the OVERDUB logo fix — safe here since it's pure black artwork with no intentional white fills, so the "O" counter and the mic icon's highlight lines correctly go transparent too) and trimmed to `images/voice-sprecherverband-logo.png`.

The source logo is black-on-transparent; on the dark card it needs `filter: brightness(0) invert(1)` (scoped to `.assoc-logo img` only, not sitewide) to render as clean white, consistent with how every other single-tone logo on the site (Hermes, Hilfswerk, etc.) is treated. `.assoc-logo-placeholder`'s fixed 80×80 dashed box was replaced with `.assoc-logo` (140px wide, `height:auto`) sized for the logo's actual ~2.15:1 aspect ratio instead of forcing it into a square.

## Favicon (2026-08-01, redesigned 2026-08-03, reverted to literal logo 2026-08-03)

Added a full favicon set to all 8 pages (index, samples, services, about, clients, contact, impressum, datenschutz — `nav.html` is a copy-paste reference snippet, not a real page, so it was left alone).

**Attempt 1 (2026-08-01) — reverted:** used the green dot-matrix "burst" mark from `images/logo.svg` as-is (the icon half of the nav lockup, extracted separately from the pink "Fresh Voices" wordmark since text isn't legible at 16–32px). Tested at 16×16 with a fake 8x device-scale-factor render and it looked fine — but that test was misleading: it rendered the SVG at ~128px and displayed the result at 16px CSS size, never actually producing a true 16×16-pixel image. Wolf reported the real favicon was "barely visible" on the site. Recoloring green→dark alone didn't fix it either (tested) — the root cause is the halftone dot texture itself: at genuine 16×16/32×32 pixel resolution, individual dots and gaps between them average into a flat, low-contrast gray smear regardless of color. Fine stippled/halftone textures fundamentally don't survive downsampling to favicon resolution.

**Attempt 2 (2026-08-03) — reverted:** a bold solid silhouette computed from the convex hull of the dot pattern's dense central cluster (circles with radius ≥ 0.95, excluding the sparse peripheral "wave tail" dots), smoothed into one closed shape via Catmull-Rom-to-Bezier conversion, filled solid `#1A1A1A`. Legible at true 16×16/32×32, but Wolf's direct feedback was "this has nothing to do with my logo" — an algorithmically-derived shape can be perfectly crisp and still read as unrelated to the actual brand mark to someone who knows it.

**Attempt 2.5 (2026-08-03) — reverted, never shipped:** a plain hand-drawn rounded diamond, on the reasoning that the dot artwork's overall silhouette is itself roughly diamond-shaped. Discarded before showing Wolf, in favor of the current approach below, since it was still a redrawn shape rather than the actual logo.

**Current version (2026-08-03):** the literal `images/logo.svg` artwork (dot-matrix mark + "Fresh Voices" wordmark, unchanged colors — green `#91C268` dots, pink `#FF63A1` text), per Wolf's explicit instruction to stop redesigning it and just use the real logo. `favicon.svg` is `images/logo.svg`'s exact inner content with only the outer `<svg>` tag's `viewBox`/`width`/`height`/`preserveAspectRatio` changed — from the original's wide `383.5224 × 192.2949` box to a square `383.5224 × 383.5224` box (vertical padding split evenly above/below), `width`/`height` `512×512`, and `preserveAspectRatio="xMidYMid meet"` (was `"none"`) so the square favicon slot can never stretch it. No paths, colors, or coordinates were touched. Raster PNGs/ICO were rendered from this same square SVG at high resolution, trimmed to the opaque bounding box, and resized down with Pillow LANCZOS. **Known tradeoff, accepted deliberately:** at true 16×16/32×32 this is nearly illegible (the halftone texture and the wordmark both collapse into a small colored smear, same underlying limit documented in attempt 1) — but that's the explicit choice here: fidelity to the real logo over small-size legibility. `apple-touch-icon.png` was **not** changed — still the original green dot-matrix mark alone on a solid `--base` background, which reads fine at its native 180×180.

Files (in `images/favicons/`, plus one at site root):

- `favicon.svg` — the actual logo (icon + wordmark), square-cropped, scalable, used by browsers that support SVG favicons
- `favicon-16x16.png`, `favicon-32x32.png`, `favicon-192x192.png` — raster fallbacks, same artwork
- `apple-touch-icon.png` (180×180) — unchanged since 2026-08-01: original green dot-matrix mark on a solid `--base` (#1A1A1A) background (iOS doesn't reliably handle transparency on home-screen icons; iOS applies its own rounded-corner mask on top, so the source is a plain square)
- `/favicon.ico` (site root, not in the favicons folder) — legacy fallback for browsers/crawlers that probe `/favicon.ico` directly regardless of `<link>` tags, same artwork

Each page's `<head>` links all of these plus a `<link rel="shortcut icon" href="favicon.ico">`. No web manifest was added — that's a bigger PWA-icon-set decision (app name, theme-color, etc.) that wasn't asked for; the 192×192 PNG is there if that's revisited later.

**Lessons for next time**:

1. When testing whether a small icon/logo will be legible at a target size, always render at the *actual* target pixel dimensions (`deviceScaleFactor: 1`, no upscaling), not a scaled-up simulation — the two can look completely different for anything with fine detail or texture.
2. Legibility and "is this actually my logo" are separate, and Wolf weighted the second one higher than the first here — don't assume small-size legibility is the dominant constraint without checking.

## WP-01 — Mobile navigation hotfix (2026-09-06)

Fixed the mobile hamburger menu, which had no way to close once opened, plus the light UA-default button boxes visible behind "EN" and the hamburger on iPhone (findings A1, A2, A4, A5, A6, D6 from the 2026-09 review).

- **Global `button` reset** added to `css/style.css`'s Reset section (`appearance:none`, `background:none`, `border:0`, `color:inherit`, `font:inherit`, `cursor:pointer`). Checked every existing button component (`.nav__lang`, `.nav__hamburger`, `.btn`, `.audio-player__play`, `.filter-btn`, `.hero__mute-btn`, the form submit button) — all already declared their own background/border explicitly, so nothing regressed; `.nav__lang` and `.nav__hamburger` got an explicit `background: transparent` added anyway for clarity.
- **Closable at last**: the hamburger now toggles an `.is-open` state (bars morph into an "×"), and the menu closes via a second tap, `Escape`, a click on empty space inside the overlay `<ul>` (`e.target === navLinks`), a `resize` past 768px, or clicking a link (already existed). `aria-expanded` and a DE/EN `aria-label` ("Menü öffnen"/"Menü schließen", read from `document.documentElement.lang`) are kept in sync.
- **Stacking fix**: `.nav__inner` now has `position:relative; z-index:101`, and `.nav__logo`, `.nav__lang`, `.nav__hamburger` each got `position:relative; z-index:100` — without this, the overlay's own `z-index:99` (needed so it paints above the page) would otherwise sit above those controls inside `.nav__inner`'s stacking context and eat their clicks. Verified via Playwright `elementFromPoint` on the hamburger's own center, post-open, on all 8 pages.
- **iOS-safe scroll lock**: replaced `document.body.style.overflow` with an `html.menu-open` class (`overflow:hidden; height:100%` on both `html` and `body`); the overlay itself got `overscroll-behavior: contain`. Scroll position is saved on open and restored via `window.scrollTo` on close.
- **Safe-area support**: `.nav` and `.nav.scrolled` now add `env(safe-area-inset-top)` to their top padding; the open overlay adds `env(safe-area-inset-top)`/`env(safe-area-inset-bottom)` via a new `--nav-h: 84px` token (`padding-top: calc(var(--nav-h) + env(safe-area-inset-top))` so links never sit under the header bar) and safe-area bottom padding. The viewport meta on all 8 HTML pages is now `width=device-width, initial-scale=1, viewport-fit=cover`.
- **Viewport units**: `index.html`'s `.hero` uses `min-height: 100vh` with a `min-height: 100svh` fallback line; the mobile overlay uses `height: 100vh; height: 100dvh;`.
- **A11y**: opening the menu sets `inert` on `<main>` and `<footer>` and moves focus to the first nav link; closing removes `inert` and returns focus to the hamburger.
- **`nav.html` brought current**: German labels with `data-de`/`data-en` (previously English-only, out of sync with every real page), and the nav CTA's inline `style="padding:10px 22px;font-size:0.7rem"` was replaced everywhere (all 8 pages + `nav.html`) with a new `.btn--nav` class in `css/style.css`. Added a header comment: `nav.html` is a reference-only copy-paste snippet, to be deleted once WP-06 introduces real templating.

**Verification**: a throwaway Playwright script (`devices['iPhone 13']`) drove all 8 pages — open via hamburger click + `elementFromPoint` confirms the hamburger itself (not the overlay) is hit-testable at its own center; a second tap closes (`.open` removed, `aria-expanded="false"`); `Escape` closes; a click on the overlay's empty area closes; `html.menu-open` is present while open; zero `pageerror` events on any page (the only console noise was `net::ERR_TUNNEL_CONNECTION_FAILED` from the sandboxed egress proxy blocking Google Fonts — unrelated to the nav JS). Desktop 1440px: `.nav__hamburger` is `display:none`, `.nav__links` is visible, and the nav CTA text color computes to `rgb(255,255,255)` (no regression on the earlier nav-contrast fix). Screenshots saved to `/home/claude/work/wp01-shots/` (`index-top.png`, `index-menu-open.png`) — not committed to the repo, local-only debugging artifacts.

**Not done here (out of WP-01 scope)**: the manual on-device iPhone Safari verification listed at the bottom of the WP-01 card ("Verifikation durch Wolf") — this session's Playwright pass is a strong proxy but isn't a substitute for that.

## WP-06 — Migrate to Eleventy: single nav/footer/head, page CSS/JS split out (2026-09-06)

Implemented Wolf's Option A decision from the WP-06 card (`docs/review-2026-09/WP-06-templating-css-hygiene.md`): moved the site from 8 hand-copy-pasted static HTML files to Eleventy (11ty 3.x) templates. Scope 1–3, 5 (prep only), 6 done; scope 4 (the `rgba()` → token batch) deliberately left for its own separate session, per the card.

- **Reference captured before touching anything**: full-page Playwright screenshots (desktop 1440×900 + `devices['iPhone 13']`) of all 8 pages served from the pre-migration repo root, plus `document.body.innerText` per page and a copy of the 8 original HTML files, all saved outside the repo (`/home/claude/work/wp06-ref/`) for post-migration comparison.
- **New structure**: `src/` holds everything Eleventy reads — 8 page templates (`src/*.njk`, one per page, each with an explicit `permalink` so output filenames are unchanged: `index.html`, `samples.html`, etc., no trailing-slash change), `src/_includes/layouts/base.njk` (the shared `<head>`/`<body>` skeleton), `src/_includes/nav.njk` and `src/_includes/footer.njk` (nav and footer now exist exactly once each — confirmed byte-identical across all 8 original pages before consolidating, so no footer-unification content decision was needed), `src/_data/site.json` + `year.js` (contact info, dynamic copyright year), `src/css/style.css` + `src/css/pages/*.css` (page-specific `<style>` blocks moved to one file per page), `src/js/main.js` + `src/js/pages/samples.js` (the samples-filter inline `<script>` moved to its own file). Static asset folders (`css/` already covered above, `js/`, `images/`, `customers/`, `audio/`, `videos/`, `favicon.ico`) moved under `src/` via `git mv` and are passed through unchanged by `eleventyConfig.addPassthroughCopy`. The 8 root HTML files and the old `nav.html` reference snippet were `git rm`'d — their content now lives in `src/`.
- **Inline styles**: added two small shared classes to remove genuinely duplicated inline `style="..."` — `.footer__location` (the footer's "Wien, Österreich" `<li>`, previously the same inline style repeated on all 8 pages, now a single declaration in `footer.njk`) and `.waveform-divider-wrap` (the identical wrapper `<div style="padding:40px 0;...">` duplicated on index/services/clients). Reduced `style="` occurrences across the built site from 47 to 36; the rest are either one-off per-page styles (not worth a new class each) or the hero waveform bars' `style="height:XX%"` (per the WP-06 card, these are treated as data, not styling, and intentionally kept).
- **`docs/`, `_unused/`, and other root Markdown files stay at the repo root** and are outside Eleventy's `src/` input, so they're never read or copied into `_site/`.
- **`/en/` preparation (scope 5, prep only)**: no `src/en/` folder or English URLs were created — see `CLAUDE.md` → Architecture, "Prepared for `/en/`" for what a future session should set up (a `src/en/` directory with its own `lang: en` data file, using Eleventy's directory-data-file cascade, plus `hreflang` tags in `base.njk` once a first English page exists).
- **Verification**: `npm run build` succeeds; `_site/` contains exactly the 8 expected HTML files plus the passed-through asset folders (66 files total) — `docs/`/`_unused/` confirmed absent. Same Playwright screenshot pass re-run against `_site/` served locally; per-page pixel-diff (Pillow `ImageChops.difference`, threshold 20/255, both viewports) ranged 0.000%–0.448%, all comfortably under the 1% acceptance bar (the small nonzero diffs are consistent with font/antialiasing and animation-timing jitter, not content changes). `document.body.innerText` matched exactly, verbatim, on all 8 pages between before/after. A link-check script confirmed all 248 local `src=`/`href=` references across the 8 built pages resolve to real files under `_site/`. The WP-01 mobile hamburger open/close test was re-run against the built `_site/index.html` (`devices['iPhone 13']`) and still passes (`aria-expanded` toggles correctly, `.open` class toggles correctly).
- **Docs**: `CLAUDE.md` (Project overview, Development, Architecture) rewritten for the Eleventy structure; a new warning added under Domain/hosting that the Cloudflare Workers build settings must be changed (Build command `npx @11ty/eleventy`, output `_site`) before/at merge, with a full step-by-step in the new `docs/review-2026-09/CUTOVER-eleventy.md`; `README.md`'s file-structure section updated to match.

**Not done here (explicitly out of WP-06 scope 1–3/5/6, tracked separately)**: the `rgba()` → design-token batch (scope 4, a separate Haiku-executor session per the card); actually creating `/en/` content (scope 5 was prep-only); the Cloudflare dashboard build-setting change itself (documented in `CUTOVER-eleventy.md`, but the actual dashboard click-through is Wolf's to do, outside what this session can reach).

## WP-02 — Contact form on a Cloudflare Worker + Turnstile + Resend (2026-09-06)

Implemented Wolf's Variante B decision from the WP-02 card (`docs/review-2026-09/WP-02-contact-form-cloudflare.md`): a dedicated Cloudflare Worker now owns the `POST /api/contact` endpoint, sitting in front of the existing Workers-static-assets deploy of the Eleventy build. Netlify Forms is fully removed from the codebase (no `data-netlify`, `netlify-honeypot`, or hidden `form-name` field remain anywhere).

- **`wrangler.jsonc`** (new, repo root): `name: "freshvoices"` (comment flags that Wolf must confirm this matches the existing Worker name in the dashboard before first deploy — a mismatch would create a second Worker instead of updating the live one), `main: "worker/index.js"`, `compatibility_date: "2026-09-01"`, `assets` binding pointed at `./_site` with `run_worker_first: ["/api/*"]` (so the Worker's own `fetch` handler sees `/api/contact` before falling through to static assets), `vars` for `CONTACT_TO`/`CONTACT_FROM`, `observability.enabled: true`. Secrets (`RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`) are documented in a comment but deliberately **not** stored in this file.
- **`worker/index.js`** (new, ES module): `export default { fetch }` routes `POST /api/contact` to the handler and hands everything else straight to `env.ASSETS.fetch(request)` — the static-assets binding, unchanged.
- **`worker/contact.js`** (new): the actual logic as a plain, testable `handleContact(request, env, { fetchImpl, now })` function — accepts JSON or form-urlencoded bodies, validates required fields (`firstName`, `lastName`, `email` with a regex check, `message`) and a 5000-char length cap on all text fields, honeypot (`bot-field` filled → silent `200 {ok:true}`, no mail sent), Cloudflare Turnstile server-side verification (`POST https://challenges.cloudflare.com/turnstile/v0/siteverify`, fails closed if the secret is missing), a best-effort per-isolate in-memory rate limit (5 requests/minute/IP — explicitly documented as *not* a durable/global limit, since Cloudflare can run multiple isolates each with their own empty Map; Turnstile is the real anti-abuse layer), and an `Origin` allowlist (`freshvoices.at`, `www.freshvoices.at`, any `*.workers.dev` preview, `localhost`/`127.0.0.1` for local dev). On success it sends mail via `POST https://api.resend.com/emails` (Bearer `RESEND_API_KEY`, `reply_to` set to the submitter's email, subject includes the chosen project type, body includes all fields + an ISO timestamp + `request.cf?.country`). Responds with JSON when the request's `Accept` header includes `application/json`; otherwise renders a small inline HTML confirmation/error page as a no-JS fallback for the plain `<form method="post" action="/api/contact">` submit path.
- **`worker/contact.test.mjs`** (new, plain `node --test`, no framework): 11 cases covering the happy path (asserts the Resend payload's `to`/`reply_to`/subject), honeypot (silent success, zero external calls), Turnstile failure → `400 {error:"captcha"}`, missing/invalid fields → `400 {error:"validation", fields:[...]}`, `GET` → `405`, disallowed vs. allowed `Origin`, a Resend-failure → `502`, the per-IP rate limit tripping on a 6th request, and the no-JS HTML fallback. `npm test` (new script, `node --test worker/*.test.mjs`) — all 11 pass.
- **`src/contact.njk`**: removed `data-netlify="true"`, `netlify-honeypot="bot-field"`, and the hidden `form-name` input; form now posts to `action="/api/contact" method="POST"` (kept as the no-JS fallback). Added the Turnstile widget (`<div class="cf-turnstile" data-sitekey="{{ site.turnstileSiteKey }}" data-theme="dark">`) before the submit button and the `challenges.cloudflare.com/turnstile/v0/api.js` script tag, with an inline comment flagging that `site.turnstileSiteKey` is currently Turnstile's published "always passes" test key and must be swapped for the real site key before go-live.
- **`src/_data/site.json`**: added `turnstileSiteKey: "1x00000000000000000000AA"` (Turnstile test key placeholder).
- **`src/js/main.js`**: submit handler now `POST`s JSON to `/api/contact` (`Accept: application/json`) instead of URL-encoding to `/`; existing per-field validation, DE/EN messages, and double-submit prevention are unchanged. A `{ok:false, error:"captcha"}` response shows a new DE/EN "Bitte Sicherheitsprüfung bestätigen" / "Please confirm the security check" message and calls `window.turnstile.reset()`; the Turnstile widget's own hidden `cf-turnstile-response` input rides along automatically as part of the form's `FormData`, so no separate token plumbing was needed. Network/other errors keep the existing fallback message and also reset Turnstile so a retry isn't blocked by a stale/used token.
- **`src/datenschutz.njk`**: the Netlify hosting and Netlify Forms paragraphs are replaced with Cloudflare (hosting/CDN, Cloudflare Inc., SCC for USA transfers), Resend (contact-form mail delivery, Resend Inc., USA, SCC), and a new paragraph on Cloudflare Turnstile (spam protection, technical data only, no tracking cookies); the honeypot paragraph is kept. Added a "Stand: September 2026" / "Last updated: September 2026" line at the end of the page — this page has never previously had one.
- **Build isolation confirmed**: `worker/` is a Node/Workers-runtime concern only — it is not under `src/`, so Eleventy's build never touches it and `_site/` does not contain it (`npm run build` output checked directly).
- **Verification**: `npm test` → 11/11 pass. `npm run build` → 8/8 pages written, no errors. A throwaway Playwright pass (iPhone 13 + desktop 1440, `_site/` served via `python -m http.server`) confirmed: `contact.html` renders, the `.cf-turnstile` div is present with the placeholder sitekey (the remote Turnstile script itself doesn't load in this sandboxed environment — expected, not a bug), submitting the empty form surfaces all five inline DE error messages and fires no request anywhere. `git grep -i "api_key\|secret"` across the changed paths turns up only variable names/comments (`RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`) and the tests' own mock string literals (`test-resend-key`, `test-turnstile-secret`) — no real secret values are committed.

**Not done here / open until cutover (see `docs/review-2026-09/CUTOVER-eleventy.md` → "Kontaktformular (WP-02)" for the full list)**: creating and verifying the Resend sending domain (DKIM/Return-Path DNS in Cloudflare, SPF include added alongside the untouched Zoho MX/SPF), creating the real Turnstile widget in the Cloudflare dashboard and swapping the site key into `site.json`, setting the two Worker secrets (`RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`) via the dashboard or `wrangler secret put`, confirming the Worker name in `wrangler.jsonc` matches the dashboard, and an actual end-to-end test submission landing in Wolf's Zoho inbox. None of this can be done from this sandboxed session (no Cloudflare login, no network egress to the real Resend/Turnstile APIs, no DNS access).

## WP-04 — Logo & brand system: clean SVGs, header/footer logo, favicon distillate, OG image (2026-09-06)

Implemented Wolf's Option A decision from the WP-04 card (`docs/review-2026-09/WP-04-logo-brand-system.md`): keep and clean up the legacy logo — the italic pink "Fresh Voices" script over the green dot cloud — rather than build a new Montserrat wordmark (Option B, the card's original recommendation).

- **Clean brand SVGs** (new `src/images/brand/`): the Wix-export source (`_unused/logo.svg`, 5000×2506.97, `preserveAspectRatio="none"`, a baked-in `<filter>` drop-shadow, 223 `<circle>` + 19 `<path>` elements gated behind CSS classes inside two nested nonstandard-viewBox `<svg>` groups) was parsed in Python: both element groups' coordinates were resolved out of their Wix nested-viewBox transforms into one shared coordinate space (`svgpathtools` computed each text path's bounding box so the two groups — dot cloud and script — could be unioned into one tight viewBox), then `svgo` (aggressive precision, single merged text path) brought each variant down to ~11.2 KB. Output: `logo.svg` (pink `#FF63A1` script / green `#91C268` dots, header), `logo-white.svg` (white script / green dots, footer), `logo-on-light.svg` (`#C93D75` script / green dots, light backgrounds) — all with `preserveAspectRatio="xMidYMid meet"`, `<title>Fresh Voices</title>`, `role="img"`, no `<filter>`/`<defs>`/CSS classes. The old `src/images/logo.svg` and `logo-white.svg` moved to `_unused/` via `git mv`.
- **Footer dot-color decision**: green dots (`#91C268`) kept for `logo-white.svg` rather than switching to grey — a grey-dot variant was rendered and compared side by side against white script text on the `--base` footer background; green read as more on-brand with equivalent legibility, so grey was discarded.
- **Header** (`src/_includes/nav.njk`): logo `<img>` now points at `images/brand/logo.svg` with `alt="Fresh Voices"` (no more "logo" suffix) and `width="66" height="40"` intrinsic attributes matching the new viewBox's aspect ratio. `src/css/style.css`: `.nav__logo img { height: 40px }` desktop, `height: 34px` added inside the existing `@media (max-width: 768px)` block; the unused `.nav__logo span { color: var(--pink) }` fallback rule removed (there was never a `<span>` in the markup — dead CSS).
- **Footer** (`src/_includes/footer.njk`): the `<div class="footer__brand-name">Fresh<span>Voices</span></div>` text fallback replaced with `<img src="images/brand/logo-white.svg" alt="Fresh Voices" height="44" width="73">` inside a new `.footer__logo` wrapper. `.footer__brand-name`/`.footer__brand-name span` CSS rules removed, replaced with `.footer__logo` (margin) and `.footer__logo img { height: 44px; width: auto }`.
- **Favicon distillate** (`src/images/favicons/favicon.svg`, 64×64, rounded `#1A1A1A` square, `rx` ≈ 18%): the card's proposed 9-dot diamond (rows 1-2-3-2-1) and its documented 5-dot cross/diamond fallback were both rendered at true 16px and 32px pixel size and visually compared — at 16px the 9-dot pattern blurs into a solid diamond-shaped blob (dots no longer individually distinguishable) and the 5-dot cross-arrangement merges into a plus sign that no longer reads as a diamond at all. A **4-dot corner-diamond** (no center dot) was tested as a third option and kept: it stays a clearly recognizable diamond of 4 distinct dots at both 16px and 32px. PNGs rendered via Playwright at 16/32/192/512, `apple-touch-icon.png` (180×180, unrounded — iOS applies its own corner rounding, full `#1A1A1A` background), and `src/favicon.ico` (16+32+48 via Pillow) generated from the same source. New `src/site.webmanifest` (name/short_name "Fresh Voices", icons 192/512, `theme_color`/`background_color` `#1A1A1A`, `display: browser`); `base.njk` gained `<link rel="manifest">` and `<meta name="theme-color" content="#1A1A1A">`.
- **OG image** (`src/images/og-image.png`, 1200×630, 18.9 KB after Pillow 256-color palette optimization): rendered via a throwaway Playwright HTML template — `#1A1A1A` background, the new `logo.svg` at ~560px wide, "VOICE OVER ARTIST · WIEN" beneath it in Montserrat 800 uppercase `#91C268` with `0.12em` letter-spacing (the woff2 loaded successfully via a `file://` `@font-face`, no systemfont fallback needed). `base.njk` gained `og:image` (absolute `https://www.freshvoices.at/images/og-image.png`), `og:image:width`/`og:image:height` (1200/630), `twitter:card` (`summary_large_image`), and `twitter:image` — placed outside the existing `{% if ogTitle %}` block so they render on all 8 pages, not just the one page (`index.njk`) that currently sets `ogTitle`.
- **`oecolution.svg` swap** (clients.html, WP-04/B5): `customers/logo_big.svg` was misdocumented in earlier `CLAUDE.md` notes as "Fresh Voices' own wordmark" — it's actually a clean vector of the **Oecolution** logo. Renamed to `src/customers/oecolution.svg` via `git mv` and wired into both the `.logo-wall` grid card and the `.ref-strip` in `src/clients.njk`, replacing the `oecolution.png` flood-fill stopgap. Rendered side by side under the site's actual logo-wall filter (`grayscale(1) brightness(1.6)`), the vector has no flood-fill halo artifacts around the letterforms that the PNG had — a clear improvement, not just a like-for-like swap. `oecolution.jpg`/`oecolution.png` stay on disk, now unreferenced.
- **Docs**: `CLAUDE.md` → Brand tokens gained a "Brand assets" paragraph (files, usage rules, minimum logo height 30px, favicon-only below 24px, no text fallback); the clients.html section's `logo_big.svg` note corrected and updated for the rename/swap. `docs/ASSETS.md` gained a new "Brand" section with the full SVG-cleanup method, the footer dot-color and favicon dot-count decisions, and the `oecolution.svg` swap rationale; its `customers/` inventory table entries for the renamed file and `oecolution.jpg`/`.png` updated. `next_tasks.md`'s favicon paragraph marked done (with the old illegible-favicon note kept as superseded history) and its logo-decision/Oecolution items closed out. WP-04 board card: decision recorded (Option A, "Wolf will das etablierte Logo behalten"), acceptance criteria checked off with two documented deviations (SVG size ~11.2 KB against the card's original ≤6 KB target — the session prompt's actual target was ≤12 KB, which was met; `git grep "logo_big"` still appears in prose history files and the untouched internal `<path id="logo_big">` attribute inside the renamed `oecolution.svg`, both non-functional).
- **Verification**: `npm run build` → 8/8 pages written, no errors. Playwright screenshots (served `_site/` locally) — iPhone 13 (390px) header: logo/EN-button/hamburger gaps measured at 186px and 16px respectively (well over the 12px minimum), so the logo did not need to shrink further to 30px; logo legible and reads as "Fresh Voices" at its 34px mobile height. Desktop 1440px header and footer both render the new logo correctly. `clients.html`'s logo wall confirmed rendering `oecolution.svg` cleanly. The WP-01 mobile-menu open/close test was re-run against the new build and still passes. `grep -rn "logo\.svg\|logo-white\.svg\|footer__brand-name" src/ _site/` returns only the new `images/brand/logo*.svg` paths and the unrelated `customers/spreadfilms-logo.svg` — no stale references. All screenshots saved to `/home/claude/work/wp04-shots/` (outside the repo, per the session's convention for review artifacts).

**Not done here**: swapping `worker`/Cloudflare-dashboard-side OG-image validation (LinkedIn Post Inspector / opengraph.xyz) — not reachable from this sandboxed session without a live deploy; Wolf should check the share preview once this branch is deployed. `AGENTS.md` (a separate, already-stale pre-Eleventy duplicate of `CLAUDE.md` for Codex) was left untouched — out of this card's scope.

## WP-04 fix — restore original logo crop, larger footer logo (2026-09-06)

Follow-up to WP-04 above, from review feedback on the header screenshot.

- **Stray dots removed**: the first WP-04 pass built each `logo.svg`/`logo-white.svg`/`logo-on-light.svg` viewBox as the tight bounding box of *every* circle and path in the Wix source. That inadvertently included 18 tiny decorative dot-shaped paths that the original file's own nested text-group `<svg>` (its own local viewBox, `1.16 -31.48 261.32 31.8`) clips into invisibility — confirmed by rendering the untouched Wix original at matched scale and comparing pixel-for-pixel, which shows none of them. Widening the viewBox to a from-scratch bounding box undid that built-in clipping, so they appeared as stray pink specks below-left of the wordmark (visible in the WP-04 header screenshot). Root-caused via a Python bbox-per-path breakdown (`svgpathtools`) comparing each path's *local* (pre-transform) coordinates against that nested viewBox, not just its transformed world position.
- **Fix applied**: viewBox reset to the Wix export's own original outer crop (`381.2388 201.8525 383.5224 192.2949`, aspect ≈ 2:1), transformed into the cleaned coordinate system → final `viewBox="0 0 383.5 192.3"`. Of the 19 original `<path>` elements, only the one merged letterform path survives the local-viewBox filter; the other 18 (all decorative, all outside the text group's own local box) are dropped entirely rather than merely clipped. All 223 dot-cloud circles were already within their own local viewBox, so none were touched. Re-optimized with the same `svgo` pass as before; all three files came out slightly *smaller* than the first pass (~10.4 KB vs. ~11.2 KB) since there's less path data to encode.
- **Footer logo enlarged**: `.footer__logo img` raised from 44px to **56px** desktop, **48px** mobile (new rule inside the existing `@media (max-width: 768px)` block); `footer.njk`'s `<img>` width/height attributes updated to `112`/`56` to match the new display size and the ~1.994:1 aspect ratio.
- **Header logo width/height corrected**: `nav.njk`'s `<img>` now uses `width="80" height="40"` (was `66`×`40`, computed against the old incorrect ~1.65:1 aspect before the crop fix; now matches the true ~1.994:1 ratio). Displayed heights (40px desktop / 34px mobile) are unchanged from the first WP-04 pass.
- **Verification**: `npm run build` clean. Playwright re-screenshots of header (iPhone 13 + 1440px), footer (desktop + mobile), and 3× closeups of both logo placements confirm no stray dots anywhere and a correct 2:1 crop; computed styles confirm 40/34px header and 56/48px footer heights render as expected. The WP-01 mobile-menu open/close test was re-run against the rebuilt site and still passes. `grep -rn "logo\.svg\|logo-white\.svg\|footer__brand-name" src/ _site/` still returns only the new `images/brand/` paths and the unrelated `spreadfilms-logo.svg`. Old (stray-dot) screenshots in `/home/claude/work/wp04-shots/` were replaced with corrected versions.

## WP-07 — Contrast, reveal timing, nav hierarchy, tap targets, QA script (2026-09-06)

Scope 1, 2, 4, 5, 6, 7 from `docs/review-2026-09/WP-07-design-polish-qa.md`. Scope 3 (hero-badge/stats, customer logo strip on the homepage) intentionally **not done** — content decision still open, tracked separately.

- **Contrast (Scope 1)**: `--muted` raised from `#9A9A9A` to `#B3B3B3` — computed contrast **8.30:1** on `--base` (`#1A1A1A`) and **6.48:1** on `--card` (`#2E2E2E`), clearing the ≥7:1 / ≥5.5:1 targets. New token `--muted-soft: #9A9A9A` (the old `--muted` value) keeps the original dimmer tone for uppercase, letter-spaced "label" typography only — `.footer__heading`, `.form-group label`, `.stat__label` (index), `.filter-sidebar__heading` (samples), `.spec__label` (services), `.fact__label` (about), `.client-group__heading` (clients), `.contact-info__label` (contact). Every other `--muted` selector (body copy, captions, footer links/copy, legal text, price notes, etc.) keeps `--muted` and gets the brighter value automatically.
- **Reveal (Scope 2)**: `src/js/main.js`'s `IntersectionObserver` now uses `{ threshold: 0.05, rootMargin: '0px 0px 15% 0px' }` (was `{ threshold: 0.12 }`) — elements fire earlier during fast scrolling. `src/css/style.css` gained `@media (max-width:768px) { .js .reveal { transform: none; transition: opacity .3s ease } }` — mobile reveal is now opacity-only (no translateY), addressing the "empty section" false-alarm pattern documented in `CLAUDE.md` under Traps that already bit.
- **Nav hierarchy (Scope 4)**: `.nav__lang` (`src/css/style.css`) changed from a solid `1px solid var(--grey-dark)` border to `1px solid transparent`, with `border-color: var(--green)` added on both `:hover` and the new `:focus-visible`. It now reads as a ghost text toggle; the "Jetzt anfragen" / "Get in touch" CTA (`.btn--primary`) remains the only filled button in the nav, on both desktop and mobile. Mobile (`≤768px`) `.nav__lang` gained `min-height: 44px`, `display: inline-flex`, `align-items: center` so the tap target clears 44px even though the visual chip stays small.
- **Tap targets ≥44×44 (Scope 5)**, measured via Playwright `getBoundingClientRect()` on iPhone 13 (390×844):
  | Element | Before | After |
  |---|---|---|
  | `.filter-btn` (samples, mobile chip) | ~36×24px (10px 14px padding, no min-height) | `min-height: 44px`, `min-width: 44px`, flex-centered — verified ≥44×44 |
  | `.audio-player__play` | 48×48px | unchanged (already compliant) |
  | `.footer__social a` | 36×36px | 44×44px (icon SVGs stay 16×16, only the box grew) |
- **QA script (Scope 6)**: new `scripts/qa-shots.js` (Node + Playwright, not copied into `_site/` since it lives outside `src/`). `npm run qa` = `npm run build && node scripts/qa-shots.js`. Spins up its own `node:http` static server for `_site/` on a free port (no Python needed). Viewports: `iphone13` (Playwright's `devices['iPhone 13']`), `iphone17` (402×874, dsf 3, custom mobile UA — no built-in Playwright device yet), `ipad` (`devices['iPad (gen 7)']`), `desktop` (1440×900). For each of the 8 pages × 4 viewports: a top screenshot, a menu-open screenshot (mobile viewports only), and a full-page screenshot with every `.reveal` forced to `.revealed` first. Checks: no horizontal overflow, `h1` top < 60% of viewport height, mobile menu open/close (hamburger click → `menu-open` class + `elementFromPoint` over the hamburger center resolves to the hamburger itself; second click closes; `Escape` closes), no uncaught `pageerror`s, no failed requests outside a whitelist regex (Google Fonts, YouTube-nocookie, Cloudflare Turnstile, `.mp3`/`.mp4`), and every `<img>` has `naturalWidth > 0` (lazy/offscreen images get `scrollIntoView()`'d first). Output: a console table, `.qa/report.json`, and screenshots under `.qa/<viewport>/`; `.qa/` added to `.gitignore`. Exit code 1 on any failing check.
- **Real-device checklist (Scope 7)**: new `docs/QA-CHECKLIST.md` — checkbox list covering iPhone Safari (menu, language toggle, audio playback, contact form + Turnstile, video autoplay/mute), Android Chrome, Desktop Safari/Chrome/Firefox, Lighthouse Mobile (≥90 Performance, ≥95 Accessibility), and a share-preview check via opengraph.xyz.

**Verification**: `npm run build && npm run qa` — **32/32 page×viewport combinations pass, exit code 0** (run inside this sandbox, `NODE_PATH` pointed at the global Playwright install; some requests to `fonts.googleapis.com`/`challenges.cloudflare.com`/`youtube-nocookie.com` are rejected by the sandbox's egress proxy as expected — they're covered by the QA script's whitelist regex and don't fail the run).

Full-page screenshot heights, before (this branch pre-WP-07) vs. after, per page — `ipad`/`desktop` are byte-for-byte unchanged (0px diff, confirming no unintended layout shift on those viewports); `iphone13`/`iphone17` grow by a consistent **+8px** (+11/12px on `samples.html`, which also has the `.filter-btn` tap-target change), fully explained by the intentional `.footer__social a` 36→44px and `.filter-btn` 44px-min-height changes surfacing in the mobile stacked layout:

| Page | iphone13 before→after | iphone17 before→after | ipad | desktop |
|---|---|---|---|---|
| index.html | 3875→3883 | 3818→3826 | 2798→2798 | 2858→2858 |
| samples.html | 4590→4601 | 4483→4495 | 3564→3564 | 3666→3666 |
| services.html | 5132→5140 | 5083→5091 | 2888→2888 | 2930→2930 |
| about.html | 4464→4472 | 4435→4443 | 3450→3450 | 2223→2223 |
| clients.html | 4069→4077 | 4069→4077 | 2990→2990 | 3111→3111 |
| contact.html | 2714→2722 | 2691→2699 | 2244→2244 | 1848→1848 |
| impressum.html | 2958→2966 | 2931→2939 | 2254→2254 | 2379→2379 |
| datenschutz.html | 4745→4753 | 4636→4644 | 3248→3248 | 3373→3373 |

Contrast values (computed via WCAG relative-luminance formula, not measured from a screenshot):
- `--muted` `#B3B3B3` on `--base` `#1A1A1A`: **8.30:1** (target ≥7:1 ✓)
- `--muted` `#B3B3B3` on `--card` `#2E2E2E`: **6.48:1** (target ≥5.5:1 ✓)
- `--muted-soft` `#9A9A9A` on `--base`: 6.19:1 (unchanged from the old `--muted` — intentional, label-only use)

**Open items**: Scope 3 (hero badge/stats finalization, customer logo strip on the homepage) is explicitly out of scope for this WP-07 pass — content decision still pending from Wolf; see the WP-07 board card. Lighthouse and real-device checks in `docs/QA-CHECKLIST.md` are unticked — they require a live/deployed URL and real hardware, neither available in this sandboxed session.

## Preview feedback (2026-09-07, iOS Safari, Cloudflare preview)

Wolf tested the Cloudflare preview on his iPhone (iOS Safari) and reported four issues, all fixed in one commit:

1. **Hero overflow on mobile Safari** — `.hero__inner`'s `grid-template-columns: 1fr` resolves to `minmax(auto, 1fr)`, and Safari (unlike Chromium) counts a `<video>`'s `poster` intrinsic width toward that `auto` minimum — with a 1280px poster, the subline and video pushed past the viewport's right edge and got clipped. Fixed in `src/css/pages/index.css`: `minmax(0, 1fr)` on `.hero__inner` (mobile single-column and desktop two-column), `min-width: 0` on `.hero__content`/`.hero__visual`, `overflow: hidden; max-width: 100%` on `.hero__photo-wrap` (mobile), `width: 100%; height: auto` on `.hero__photo` (mobile, keeping `max-height: 45svh` + `object-fit: cover`), and `overflow-wrap: anywhere` on `.hero__subtitle`. Chromium (the only engine this sandbox could install — `npx playwright install webkit` is blocked by the network allowlist) doesn't reproduce the underlying quirk itself (it doesn't use the poster for intrinsic sizing), so the fix couldn't be verified as a before/after regression in this sandbox; verified instead that `minmax(0,1fr)` doesn't change Chromium's layout (`maxRight === innerWidth` before and after) and added a WebKit-agnostic guard: `scripts/qa-shots.js` now has a `hero-no-right-overflow` check that walks every element inside `.hero` and asserts `getBoundingClientRect().right <= innerWidth + 1`, run for every page/viewport (not just index) — this would have caught the original bug on any engine that does reproduce it.
2. **Motto image on about.html** — the Hemd photo (`motto-192.jpg`/`.webp`) is replaced with an original SVG illustration, `src/images/motto-freeway-sign.svg` (~1.7 KB): a California freeway guide-sign referencing the *L.A. Story* quote it sits next to (no film still, no real signage/branding). `.motto` changed from a row layout (image left, text right) to a column layout (image on top, quote below), on both mobile and desktop, so the card no longer needs the old fixed 96px thumbnail sizing. Old files moved to `_unused/` via `git mv`. Details in `docs/ASSETS.md`.
3. **Turnstile widget visible pre-interaction** — `data-appearance="interaction-only"` added to `.cf-turnstile` in `src/contact.njk`, so the widget only renders once a challenge is actually needed. The "For testing only" banner still shown in the current preview is a property of Turnstile's public test site key (`1x00000000000000000000AA`), not of this markup — noted in `docs/review-2026-09/CUTOVER-eleventy.md`, it goes away once the real site key from Cloudflare is in place.
4. **Form feedback not visible after submit** — (a) `src/js/main.js`'s `showFeedback()` now blurs the active field (closes the iOS keyboard), scrolls the feedback element into view (`scrollIntoView({block:'center', behavior:'smooth'})`), and focuses it (`tabindex="-1"`, `focus({preventScroll:true})`) so assistive tech and keyboard/scroll position both land on it. (b) New `not_configured` error path: `worker/contact.js` now checks `env.RESEND_API_KEY`/`env.TURNSTILE_SECRET_KEY` before the Turnstile network round-trip and returns 503 `{ok:false,error:'not_configured'}` (or the HTML no-JS fallback) plus a `console.warn` for the Cloudflare log, if either secret is missing — covers the pre-cutover state where the form looks broken with no clear reason. Frontend shows a bilingual message pointing at `wolf.valtiner@freshvoices.at` directly. Two new tests in `worker/contact.test.mjs` (missing `RESEND_API_KEY`, missing `TURNSTILE_SECRET_KEY` → 503, zero Turnstile/Resend calls) — `npm test` is now 13/13.

Verified: `npm test` (13/13), `npm run build`, `npm run qa` (32/32 page×viewport, 0 failures, including the new hero check). Screenshots (iPhone 13, via the QA harness's own Chromium/Playwright setup) under `/home/claude/work/p16-shots/`: `index-top-iphone13.png` (subline + video fully inside the frame), `about-motto-card-iphone13.png` (new sign illustration), `contact-feedback-not-configured-iphone13.png` (feedback box visible mid-viewport after a mocked `503 not_configured` response via `page.route`).
