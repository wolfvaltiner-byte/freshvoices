{
  "name": "freshvoices",
  "version": "1.0.0",
  "description": "freshvoices.at — Wolf Valtiner Voice Over, static site built with Eleventy",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "node --test worker/*.test.mjs",
    "qa": "npm run build && node scripts/qa-shots.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wolfvaltiner-byte/freshvoices.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "playwright": "^1.56.0"
  }
}
